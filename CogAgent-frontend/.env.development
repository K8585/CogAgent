# 开发期：Vite devServer 把 /api 代理到这个地址（绕过跨域）
# 后端默认监听 8080，无 context-path
VITE_PROXY_TARGET=http://localhost:8080

# 开发期请保持为空：留空时 axios 使用同源相对路径 /api/v1/...，
# 由 Vite 代理转发；填了值反而会变成浏览器直连、触发跨域。
VITE_API_BASE_URL=
